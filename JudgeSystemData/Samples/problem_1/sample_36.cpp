#include <iostream>

using namespace std;

int main() {
    // Khai báo 2 biến nguyên a và b
    long long a, b; 
    
    // Yêu cầu người dùng nhập dữ liệu (nếu làm trên các trang chấm code online thì không cần dòng cout này)
    // cout << "Nhap a va b: "; 
    
    // Đọc giá trị a và b từ bàn phím
    cin >> a >> b;
    
    // In ra màn hình tổng của a và b
    cout << a + b << endl;
    
    return 0;
}